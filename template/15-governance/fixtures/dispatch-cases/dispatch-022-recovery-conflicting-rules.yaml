case_id: dispatch-022-recovery-conflicting-rules
scenario: Conflicting rule evidence during verification run
mode: diagnostic
dispatch_tuple:
  intent_type: verify
  intent_state: blocked
  ontology_entity: Rule
  target_scope: rules
  mutation_class: read-only
  risk_level: medium
  evidence_state: conflicting
  actor_mode: human-led
expected_output:
  primary_pipe: PipeRecovery
  applicable_rules: [KBX-M001, KBX-M002, KBX-M003, KBX-V001, KBX-V002, KBX-WF011]
  required_gates: [P2, P19]
  allowed_actions: [collect-evidence, request-recheck]
  verification_requirements: [conflict-resolution-required]
  fallback_or_escalation: null
explainability:
  tuple_to_pipe_basis: [evidence_state=conflicting]
  tuple_to_rule_basis: [intent_type=verify, target_scope=rules]
determinism_checks:
  repeated_resolution_same_output: true
  no_random_branching: true
acceptance:
  resolves_to_single_pipe_or_human_gate: true
  conditional_rules_tuple_based: true
  no_state_mutation: true
