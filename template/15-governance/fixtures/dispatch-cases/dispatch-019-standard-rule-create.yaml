case_id: dispatch-019-standard-rule-create
scenario: Create a new rule metadata contract in rules scope
mode: execution
dispatch_tuple:
  intent_type: create
  intent_state: draft
  ontology_entity: Rule
  target_scope: rules
  mutation_class: contract-changing
  risk_level: medium
  evidence_state: sufficient
  actor_mode: human-led
expected_output:
  primary_pipe: PipeStandard
  applicable_rules: [KBX-M001, KBX-M002, KBX-M003, KBX-I001, KBX-GB001, KBX-WF008, KBX-KA103]
  required_gates: [P2, P10, P19]
  allowed_actions: [create-rule, register-routing]
  verification_requirements: [metadata-schema-valid]
  fallback_or_escalation: null
explainability:
  tuple_to_pipe_basis: [intent_type=create, mutation_class=contract-changing]
  tuple_to_rule_basis: [ontology_entity=Rule]
determinism_checks:
  repeated_resolution_same_output: true
  no_random_branching: true
acceptance:
  resolves_to_single_pipe_or_human_gate: true
  conditional_rules_tuple_based: true
  no_state_mutation: true
