case_id: dispatch-010-human-gate-autonomous-contract
scenario: Agent-autonomous actor attempts contract-changing update
mode: execution
dispatch_tuple:
  intent_type: update
  intent_state: active
  ontology_entity: Rule
  target_scope: rules
  mutation_class: contract-changing
  risk_level: medium
  evidence_state: sufficient
  actor_mode: agent-autonomous
expected_output:
  primary_pipe: null
  applicable_rules: [KBX-M001, KBX-M002, KBX-M003, KBX-V001, KBX-I001, KBX-WF008]
  required_gates: [P2, P10, P20]
  allowed_actions: [request-human-gate]
  verification_requirements: [autonomous-mutation-approval]
  fallback_or_escalation: HumanGateRequired
explainability:
  tuple_to_pipe_basis: [actor_mode=agent-autonomous and mutation_class!=docs-only]
  tuple_to_rule_basis: [ontology_entity=Rule, target_scope=rules]
determinism_checks:
  repeated_resolution_same_output: true
  no_random_branching: true
acceptance:
  resolves_to_single_pipe_or_human_gate: true
  conditional_rules_tuple_based: true
  no_state_mutation: true
